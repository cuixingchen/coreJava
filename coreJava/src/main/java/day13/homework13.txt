作业：
  1 研究下线程状态图，总结下线程。
  2 把IO的代码和套路熟悉一下，代码要求数量起来。
  3 编程：
    a 实现聊天记录的功能，用BufferedReader读键盘输入(System.in)，不要使用Scanner，然后追加输出到某个文件中，输入bye退出。
    b 实现一下用户的注册和登录的完整版。用BufferedReader提供输入，用户信息包括用户名、密码、email，每个用户逐行写入一个文件(追加)。用户名要求唯一标识。
提示：先做注册，有时间再做登录。
  注册时，首先判断一下文件是否存在，如果不存在，新建一个文件。输入用户信息后，先从文件读入已有用户的信息(每个用户一行)，用户信息可以用,分开。把信息拆分后封入User对象，多个User对象放入集合(List/Set/Map都可以的)，然后判断用户名是否有重复，没有重复写入文件，有重复提示用户重新注册。