作业：
 1 数量使用数组，理解下标的作用，掌握今天的代码
 2 编程：
   a 分别实现选择排序和插入排序。
   b 用Scanner输入某班同学的名字和考试成绩，并且用两个数组分别保存，保存后，统计 全班的总分、平均分、最高分、最低分。并且打印出最高分和最低分的同学的名字。
  ps:先输入班级人数，后定义数组，并输入名字和成绩。
   c 写一个方法，实现数组的自增长度。定义一个int[]，可以无限制的放入int。
  public class MyArray{
    int index = 0;//记录元素个数
    int[] arr = new int[10];
    public void add(int a){
        //可以无限制的放int
    }
  }
  ps:当放入第11个时，arr = new int[新长度];，然后把原有数据进行数组拷贝，拷贝到新数组中。

   d 写一个模拟的彩票生成程序，生成7个1-33无重复的随机数。生成随机数代码如下：
   import java.util.Random;
   
   Random r = new Random();
   int ran = r.nextInt(33)+1;//1-33的随机数

  如果过滤重复？ 用int[]保存前面的数字，然后与前面数字比较，如果有重复，重新生成。
  


