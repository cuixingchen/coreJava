作业：
  1 梳理IO流，熟悉常用的IO流的代码套路。
  2 看网络编程的基本例子，代码能写。
  3 编程题：
    a 继续完成登录、注册作业。
    b 写一个文件的分割器代码。包括2个部分：文件分割、文件合并。文件分割时，输入文件名和大小，按照大小拆分文件。注意：拆分后的文件记录次序，拆分时1个输入流操作大文件，n个输出流操作每个小文件。
   合并时，输入文件名即可。注意：合并文件时要按次序合并，合并时n个输入流，每个小文件一个输入流，1个输出流操作大文件。(如何解决合并文件个数不确定问题？)
   1 new File() if(文件不存在) 退出
   2 用FileNotFoundException代表文件已合并完毕。



